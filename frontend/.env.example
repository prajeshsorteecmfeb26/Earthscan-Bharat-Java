# Vite reads this at BUILD time and inlines the value into the bundle, so
# changing it requires a rebuild — it is not a runtime setting.
#
# Point it at the API gateway, never at an individual service.
VITE_API_BASE_URL=http://localhost:8080
