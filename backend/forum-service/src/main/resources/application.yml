server:
  port: ${SERVER_PORT:8083}

spring:
  application:
    name: forum-service

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/earthscan_forum}
      auto-index-creation: true   # Creates the @Indexed / @CompoundIndex definitions on startup.

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
    publisher-confirm-type: correlated
    publisher-returns: true
    listener:
      simple:
        acknowledge-mode: auto
        retry:
          enabled: true
          initial-interval: 2s
          max-attempts: 4
          multiplier: 2.0
          max-interval: 20s
        default-requeue-rejected: false

earthscan:
  jwt:
    secret: ${JWT_SECRET:ChangeMeInProductionEarthScanBharat2026SigningKey}
    issuer: ${JWT_ISSUER:EarthScanBackend}
    audience: ${JWT_AUDIENCE:EarthScanUsers}
    expiration-ms: ${JWT_EXPIRATION_MS:604800000}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
  instance:
    prefer-ip-address: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{correlationId:-}] [user:%X{userId:-}] %logger{36} - %msg%n"
  level:
    root: INFO
    com.earthscan: ${LOG_LEVEL_EARTHSCAN:INFO}
    org.springframework.data.mongodb.core.MongoTemplate: ${LOG_LEVEL_MONGO:WARN}

# ===========================================================================
# PROFILES
# ===========================================================================
---
spring:
  config:
    activate:
      on-profile: dev
logging:
  level:
    com.earthscan: DEBUG
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

---
spring:
  config:
    activate:
      on-profile: test
  rabbitmq:
    listener:
      simple:
        auto-startup: false
  cloud:
    discovery:
      enabled: false
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false
earthscan:
  jwt:
    secret: TestOnlySigningKeyForEarthScanBharatIntegrationTests2026
logging:
  level:
    root: WARN
    com.earthscan: INFO

---
spring:
  config:
    activate:
      on-profile: prod
logging:
  level:
    root: WARN
    com.earthscan: INFO
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: never
      probes:
        enabled: true
